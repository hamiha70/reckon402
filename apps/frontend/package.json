{
  "name": "@reckon402/frontend",
  "version": "0.0.1",
  "private": true,
  "description": "Reckon402 L4c onboarding + dashboard frontend (vanilla TS + fetch polling). Served as static assets by the onboard-orchestrator Worker.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs --watch"
  }
}
