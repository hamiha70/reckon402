# Reckon402 Demo Screenshots

Screenshots captured from the live Reckon402 deployment for the ETHGlobal OpenAgents 2026 submission.

| File | Description |
|------|-------------|
| `01-landing-page.png` | Landing page at reckon402.com showing the x402 / ERC-8004 / ENS value proposition and "How It Works" steps. |
| `02-onboarding-form.png` | **PENDING RE-TAKE against the L4d 6-step flow with seller11.** Current image is from an L4c-era 5-step run with discount-tier UI and an older Splitter BPS split — the canonical narrative is now the 6-step Escrow flow with 87/3/10% Splitter routing into a per-agent Escrow. |
| `03-dashboard-trust-tier.png` | **PENDING RE-TAKE against seller11 dashboard.** Current image shows an L4c-era dashboard with discount-pricing UI ("gold 15% off" badge); the canonical seller11 dashboard shows the per-agent Escrow trust panel with `attestationCount`, active tier badge, `currentlyHeld`, `withdrawableNow`, and the `LinearMonotonicTierStrategy v1` tier table. |
| `04-dashboard-call-log.png` | **PENDING VISUAL VERIFY (or re-take) against seller11.** Dashboard scrolled to show the "Recent Paid Calls" table with settle-tx and attest-tx on-chain links, plus the terminal pane at the bottom. Call-log shape is L4d-compatible; verify the agent ENS shown is `seller11.reckon402-test.eth` and re-take if the image still references an L4c agent. |
| `05-agent-402-gate.png` | Raw JSON 402 response from agent.reckon402.com/research — proves the x402 payment gate is live and enforced. |
