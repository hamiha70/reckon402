# L0 smoke-test run — 2026-04-27

Spec: `specs/00-l0-smoke-tests.md`. Aggregator: `tools/smoke-tests/run-all.sh`.

## Invocation

```
unset AWS_PROFILE && \
  infisical run \
    --projectId 84d8a29b-27e3-46d0-bf72-bbe01215ac35 \
    --env dev \
    --domain https://secrets.intentralabs.com \
    -- ./tools/smoke-tests/run-all.sh
```

`AWS_PROFILE` is unset deliberately so `AWS_ACCESS_KEY_ID` and
`AWS_SECRET_ACCESS_KEY` (hydrated from Infisical for the
`reckon402-signer` IAM user) win in the AWS SDK credential chain.

10 secrets injected by Infisical (4 RPC URLs, 5 KMS-related, 1
ENS placeholder).

## Result

| probe | status | duration | note |
|-------|--------|----------|------|
| infisical | PASS | 1511 ms | host=secrets.intentralabs.com project=84d8a29b... |
| cf | PASS | 2757 ms | account=0f38f8667bbcbe4f54eda13c8df009e0 ns=cloudflare pending=apex-A-missing,agent-missing,facilitator-missing,gateway-missing,signing-missing,demo-missing |
| aws | PASS | 1617 ms | caller=reckon402-signer alias=evm eoa=0x46bbb05aca9ea24118b8a57c8d3f317503384305 |
| rpc | PASS | 2478 ms | PRIMARY=130ms, FALLBACK=190ms, PRIMARY=122ms, FALLBACK=204ms |
| ens | SKIP | 1702 ms | open question Q-L0-1 (ENS test name + resolver) unresolved |
| erc8004 | SKIP | 1171 ms | open question Q-L0-2 (Base mainnet registry address) unresolved |
| kh | SKIP | 436 ms | KEEPERHUB_API_KEY not yet provisioned (defer to L4) |
| d1 | PASS | 7240 ms | INSERT+SELECT roundtrip 2403 ms (well under 5 s gate) |

**Total wall time:** 18 993 ms.
**Outcome:** ALL PASS (or SKIP). Five hard requirements pass; three
optional probes skip cleanly on already-tracked open questions / a
deferred dependency. Layer L0 is recordable end-to-end.

## State at run time

### Cloudflare

- Account `0f38f8667bbcbe4f54eda13c8df009e0` reachable via OAuth
  session (token permissions include workers, workers_kv,
  workers_routes, workers_scripts, workers_tail, d1).
- `reckon402.com` registered, NS = `ada.ns.cloudflare.com` /
  `mustafa.ns.cloudflare.com`.
- No subdomain A records yet (provisioning lands in L1 alongside
  `agent.reckon402.com` worker deploy).
- `wrangler deploy --dry-run` against
  `tools/smoke-tests/cf-placeholder/wrangler.toml` succeeds.

### AWS

- Account `975170806362` (intentralabs).
- KMS key `5a0350e0-d502-4579-8d45-d31c843a5f3f` aliased
  `alias/reckon402/mainnet/buyer-signer/evm` in `eu-central-1`.
- EOA `0x46bbb05aca9ea24118b8a57c8d3f317503384305`.
- IAM user `reckon402-signer` (`AKIA6GDFVUJNDYJMYDGX`) with the
  scoped `reckon402-mainnet-buyer-signer-kms-policy` (kms:Sign +
  kms:GetPublicKey on the key ARN only).
- KMS sign-and-recover round-trip verified under both
  `intentra-admin` and `reckon402-signer` credentials by
  `tools/smoke-tests/lib/kms-verify.mjs`.

### RPC

- Base mainnet primary: Alchemy → 130 ms.
- Base mainnet fallback: `https://mainnet.base.org` → 190 ms.
- Base Sepolia primary: Alchemy → 122 ms.
- Base Sepolia fallback: `https://sepolia.base.org` → 204 ms.
- All four return the expected chain ID hex (0x2105 / 0x14a34).

### Infisical

- Self-hosted at `https://secrets.intentralabs.com`.
- Project `reckon402` (slug `reckon402`, ID
  `84d8a29b-27e3-46d0-bf72-bbe01215ac35`), env `dev`.
- 10 secrets currently set: AWS access key + secret + EOA + KMS
  identifiers, 4 RPC URLs, 1 ENS placeholder.

### D1

- Database `reckon402-d1-smoke-test` created on first run; persisted
  for re-runs (idempotent CREATE).
- Schema `l0(k TEXT PRIMARY KEY, v TEXT, ts INTEGER)`.
- Round-trip INSERT+SELECT 2 403 ms — well under the 5 s remote-D1
  gate.

## Open questions still tracked

- **Q-L0-1.** ENS test name + Sepolia resolver wiring.
- **Q-L0-2.** ERC-8004 reputation registry contract address on
  Base mainnet.
- **Q-L0-3.** Cloudflare scoped API token vs. existing OAuth session
  (deferred — L0 unblocks via OAuth).
- **Q-L0-4.** KeeperHub API access (deferred — L4).

These do not block L0 sign-off. They become blocking gates for
their respective downstream layers (Q-L0-1 for L1 ENS routing,
Q-L0-2 for L4 reputation reads, Q-L0-4 for L4 KH integration).
