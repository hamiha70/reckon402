# L0 smoke-test run — 2026-04-27

Spec: `specs/00-l0-smoke-tests.md`. Aggregator: `tools/smoke-tests/run-all.sh`.

This file is overwritten on each L0 verify commit during the build window;
git history preserves the per-commit snapshots.

## Invocation

```
unset AWS_PROFILE && \
  infisical run \
    --projectId 84d8a29b-27e3-46d0-bf72-bbe01215ac35 \
    --env dev \
    --domain https://secrets.intentralabs.com \
    -- ./tools/smoke-tests/run-all.sh
```

`AWS_PROFILE` is unset deliberately so `AWS_ACCESS_KEY_ID` and
`AWS_SECRET_ACCESS_KEY` (hydrated from Infisical for the
`reckon402-signer` IAM user) win in the AWS SDK credential chain.

17 secrets currently injected by Infisical (5 KMS-related, 8 RPC URLs,
2 ENS records, 2 funder credentials).

## Result

| probe | status | duration | note |
|-------|--------|----------|------|
| infisical | PASS | 3234 ms | host=secrets.intentralabs.com project=84d8a29b... |
| cf | PASS | 3338 ms | account=0f38f8667bbcbe4f54eda13c8df009e0 ns=cloudflare pending=apex-A-missing,agent-missing,facilitator-missing,gateway-missing,signing-missing,demo-missing |
| aws | PASS | 1960 ms | caller=reckon402-signer alias=evm eoa=0x46bbb05aca9ea24118b8a57c8d3f317503384305 |
| rpc | PASS | 4728 ms | base-main-P=141ms, base-main-F=224ms, base-sep-P=135ms, base-sep-F=214ms, eth-main-P=115ms, eth-main-F=115ms, eth-sep-P=99ms, eth-sep-F=139ms |
| ens | PASS | 709 ms | resolved=0xD53ffac42496d73B3Faf946786688a8454F57b1f |
| erc8004 | SKIP | 2193 ms | open question Q-L0-2 (Base mainnet registry address) — deferred to L3 |
| kh | PASS | 1759 ms | user=hamiha70@gmail.com org=d9dca912-bb60-41b5-a727-ee5060c2d3b1 chains=20 cli=v0.9.0 auth=token |
| d1 | PASS | 6525 ms | INSERT+SELECT roundtrip 2205 ms (well under 5 s gate) |
| funded | PASS | 1540 ms | base-sep=1.0000eth eth-sep=1.0000eth floor=0.01eth deployer=0x66c2858d9a8605957c516a77262eb66ee6be113c |

**Total wall time:** 26 083 ms.
**Outcome:** ALL PASS (or SKIP). 8 of 9 probes green; only
`erc8004` skips, gated on Q-L0-2 (registry address sourcing,
deferred to L3 when ERC-8004 reads matter for the build).
Layer L0 is fully recordable end-to-end.

## What changed since the previous snapshot

- **`ens.sh` flipped SKIP → PASS, closing Q-L0-1.** Operator
  registered `reckon402-test.eth` on Sepolia from the
  x402commit-funder (`0x9AF7…3F04`); CLI then `cast send`-ed
  `setAddr(node, seller_eoa)` on the Sepolia PublicResolver
  (`0xE99638b4…E49b5`) in tx
  [`0xd9725e9c…121d59`](https://sepolia.etherscan.io/tx/0xd9725e9c92c20be8b9788e5caae4711c63c36ef5efb7e91dcd56e4e9ed121d59).
  `ENS_TEST_NAME` and `ENS_EXPECTED_ADDRESS` pushed to Infisical;
  `ens.sh` now resolves `reckon402-test.eth` → `0xD53f…7b1f` in
  709 ms.
- **`X402COMMIT_FUNDER_PK` + `X402COMMIT_FUNDER_ADDRESS` pushed
  to Infisical.** Sensitive funder PK staged via tmpfs file
  + `infisical secrets set --file` so the value never crossed
  argv. Recipes (`tools/funding/seed-deployer.md`) now hydrate
  the funder PK directly from Infisical rather than
  awk-extracting from the operator's local x402commit secrets
  file.
- **`funded.sh` added as probe 9 (previous round).** Asserts the
  deployer EOA holds ≥ 0.01 ETH on each Sepolia. Companion to
  `aws.sh`: aws proves the signature is valid, funded proves
  it'll land on chain.
- **Deployer funded on both Sepolias (previous round).** 1 ETH
  each via cast send from x402commit-funder. Pre/post snapshots
  + tx hashes in `tools/funding/results-{pre,post}-funding-2026-04-27.md`.
- **rpc.sh extended from 4 endpoints to 8.** Adds Ethereum mainnet
  primary+fallback and Ethereum Sepolia primary+fallback to the
  existing Base coverage. Keeps the same `<200 ms primary` /
  `no-gate fallback` contract.
- **kh.sh promoted from SKIP → PASS.** Operator authenticated the
  KeeperHub CLI via `kh auth login`; the probe now verifies CLI
  presence, OAuth keyring auth, and `kh doctor --json` health on
  CLI Version + Auth + API + Chains. Wallet entry permitted as
  `warn` (creator-wallet REST requires extra auth not used at L0).
- **ens.sh hydration rekeyed.** Previously read `SEPOLIA_RPC`; now
  reads `ETH_SEPOLIA_RPC_PRIMARY` to align with the rpc.sh §4 key
  set (single source of truth for "Ethereum Sepolia primary").

## State at run time

### Cloudflare

- Account `0f38f8667bbcbe4f54eda13c8df009e0` reachable via scoped
  API token `CLOUDFLARE_API_TOKEN` (account-level scopes:
  Workers Scripts R+W, D1 R+W, Workers Tail R; zone-level scopes
  for `reckon402.com`).
- `reckon402.com` registered, NS = `ada.ns.cloudflare.com` /
  `mustafa.ns.cloudflare.com`.
- No subdomain A records yet (provisioning lands in L1 alongside
  `agent.reckon402.com` worker deploy).
- `wrangler deploy --dry-run` against
  `tools/smoke-tests/cf-placeholder/wrangler.toml` succeeds.

### AWS

- Account `975170806362` (intentralabs).
- KMS keys (`eu-central-1`):
  - `alias/reckon402/mainnet/buyer-signer/evm` →
    EOA `0x46bbb05aca9ea24118b8a57c8d3f317503384305`
  - `alias/reckon402/mainnet/deployer/evm` →
    EOA `0x66c2858d9a8605957c516a77262eb66ee6be113c`
- IAM user `reckon402-signer` (`AKIA6GDFVUJNDYJMYDGX`) — scoped
  KMS policy on the buyer-signer ARN only. The `aws.sh` probe
  continues to verify under `reckon402-signer` (least privilege).
- KMS sign-and-recover round-trip verified by
  `tools/smoke-tests/lib/kms-verify.mjs`.

### RPC (now 8 endpoints, 4 chains × 2 tiers)

- `BASE_MAINNET_RPC_PRIMARY` = Alchemy → 0x2105, 129 ms.
- `BASE_MAINNET_RPC_FALLBACK` = `https://mainnet.base.org` → 0x2105,
  258 ms.
- `BASE_SEPOLIA_RPC_PRIMARY` = Alchemy → 0x14a34, 129 ms.
- `BASE_SEPOLIA_RPC_FALLBACK` = `https://sepolia.base.org` → 0x14a34,
  314 ms.
- `ETH_MAINNET_RPC_PRIMARY` = Alchemy → 0x1, 180 ms.
- `ETH_MAINNET_RPC_FALLBACK` = `https://ethereum.publicnode.com` →
  0x1, 149 ms.
- `ETH_SEPOLIA_RPC_PRIMARY` = Alchemy → 0xaa36a7, 176 ms.
- `ETH_SEPOLIA_RPC_FALLBACK` = `https://ethereum-sepolia.publicnode.com`
  → 0xaa36a7, 171 ms.

All four primaries land under the 200 ms gate. All four fallbacks
respond cleanly with the correct chain ID (no latency gate).

### KeeperHub

- CLI v0.9.0 on PATH.
- OAuth (device-code) session in OS keyring; `auth.method=token`.
- Operator `hamiha70@gmail.com`, org `d9dca912-bb60-41b5-a727-ee5060c2d3b1`.
- `kh doctor` reports CLI / Auth / API / Chains all `pass`; Wallet
  is `warn` (creator-wallet REST requires extra auth — out of scope
  at L0).

### Infisical

- Self-hosted at `https://secrets.intentralabs.com`.
- Project `reckon402` (slug `reckon402`, ID
  `84d8a29b-27e3-46d0-bf72-bbe01215ac35`), env `dev`.
- 17 secrets currently set:
  - **AWS** (5): access key + secret + 2 KMS aliases + 2 KMS-derived EOAs.
  - **RPC** (8): 4 chains × primary+fallback (Base + Ethereum, mainnet+Sepolia).
  - **Cloudflare** (2): scoped API token + account ID.
  - **ENS** (2): `ENS_TEST_NAME=reckon402-test.eth`,
    `ENS_EXPECTED_ADDRESS=0xD53f…7b1f`.
  - **Funder** (2): `X402COMMIT_FUNDER_PK` (sensitive),
    `X402COMMIT_FUNDER_ADDRESS=0x9AF7…3F04` (external account
    used for Sepolia ETH seed + ENS registration).

### D1

- Database `reckon402-d1-smoke-test` created on first run; persisted
  for re-runs (idempotent CREATE).
- Schema `l0(k TEXT PRIMARY KEY, v TEXT, ts INTEGER)`.
- Round-trip INSERT+SELECT 2 204 ms — well under the 5 s remote-D1
  gate.

### Funding (probe 9)

- Deployer EOA `0x66c2858d9a8605957c516a77262eb66ee6be113c`
  holds 1 ETH on each of Base Sepolia and Ethereum Sepolia after
  the operator-authorized `cast send` round on 2026-04-27 ~14:20 CET.
- Source: x402commit-funder `0x9AF7...3F04` (still has 2.5 BSep /
  2.9 SepETH after the dispatch + ENS registration + setAddr).
- Floor 0.01 ETH gives ~100× headroom over current state and
  ~10× headroom over a worst-case Splitter deploy at 10 gwei
  (full math in spec §9).
- Tx hashes:
  - Base Sepolia `0x59539921...794c6f` (block 40762066, ETH seed)
  - Ethereum Sepolia `0x8de48510...6338d31` (block 10742544, ETH seed)
  - Ethereum Sepolia `0xd9725e9c...121d59` (block 10742776, ENS setAddr)

### ENS (Q-L0-1 closed)

- Name: `reckon402-test.eth` registered on Sepolia by funder
  (1-year lease via app.ens.domains; unwrapped, registry.owner ==
  funder).
- Resolver: PublicResolver `0xE99638b40E4Fff0129D56f03b55b6bbC4BBE49b5`
  (Sepolia default).
- `addr()` (coinType 60 = ETH): `0xD53f…7b1f` (the reckon402
  seller EOA). Set via direct `cast send setAddr(...)` from the
  funder's PK in tx `0xd9725e9c...121d59`.
- Live resolution verified: `viem.getEnsAddress({chain: sepolia,
  name: "reckon402-test.eth"})` → `0xD53f…7b1f`.

## Open questions still tracked

- **Q-L0-2.** ERC-8004 reputation registry contract address on
  Base mainnet. Blocking erc8004.sh PASS; deferred to L3 when the
  build actually starts reading attestations. Until then,
  `erc8004.sh` SKIPs cleanly and the SKIP entry carries an
  explicit pointer back to Q-L0-2 in the spec.

Q-L0-1 (ENS test name) closed in the same commit chain that
landed this snapshot. Q-L0-3 (Cloudflare token scope) and Q-L0-4
(KH API access) closed in earlier commits. L0 is now fully green
on every probe whose disposition has resolved.
